---
name: fiji-macro-workflow
description: 'Create, inspect, transfer, edit, run, diagnose, and verify ImageJ macros through fiji-mcp. Use for macro recording, built-in function discovery, .ijm authoring, dialogs, and macro execution diagnostics. Requires a running Fiji application with the fiji-mcp server connected.'
argument-hint: 'Describe the macro workflow, commands to record, or macro behavior to verify'
user-invocable: true
disable-model-invocation: false
---

# Fiji Macro Workflow

This skill requires a running Fiji application with the `fiji-mcp` server connected
and an agent with access to the `fiji-mcp` tools. It is a routing stub for the
Fiji-MCP workflow, not a standalone macro guide.

Before carrying out this workflow, read the complete macro workflow guidance with
the `fiji_guide_read` tool using Guide ID `creating-macros`.